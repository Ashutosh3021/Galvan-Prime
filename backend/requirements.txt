# Web framework
fastapi==0.110.3
uvicorn[standard]==0.29.0
aiosqlite==0.20.0

# Database (async)
sqlalchemy==2.0.30
asyncpg==0.29.0
alembic==1.13.1

# Auth & Security
python-jose[cryptography]==3.3.0
bcrypt==5.0.0
python-multipart==0.0.9

# Config
pydantic==2.7.1
pydantic-settings==2.2.1
pydantic[email]==2.7.1

# HTTP client (health checks + URL ingestion)
httpx==0.27.0

# Phase 2 — Ingestion
pypdf==4.2.0              # PDF text extraction
beautifulsoup4==4.12.3    # HTML/URL scraping
requests==2.31.0          # sync HTTP for URL loader
langchain-text-splitters==0.2.0  # RecursiveCharacterTextSplitter (fixed chunking)
sentence-transformers==2.7.0     # all-MiniLM-L6-v2 embeddings
chromadb==0.5.0                  # local vector store
aiofiles==23.2.1                 # async file I/O for uploads

# Testing
pytest==8.2.0
pytest-asyncio==0.23.7

# Dev utilities
python-dotenv==1.0.1
