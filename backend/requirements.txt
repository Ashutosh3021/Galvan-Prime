# Web framework
fastapi==0.110.3
uvicorn[standard]==0.29.0

# Auth & Security
python-multipart==0.0.9

# Config
pydantic-settings==2.2.1
pydantic[email]==2.7.1

# HTTP client (health checks + URL ingestion)
httpx==0.27.0

# Phase 2 — Ingestion
pypdf==4.2.0              # PDF text extraction
beautifulsoup4==4.12.3    # HTML/URL scraping
requests>=2.32.2,<3       # sync HTTP for URL loader
langchain-core>=0.3.42,<0.4.0   # anchor — shared by all langchain-* below
langchain-text-splitters>=0.3.0,<1.0.0  # RecursiveCharacterTextSplitter
sentence-transformers==2.7.0     # all-MiniLM-L6-v2 embeddings (PAID tier)
fastembed==0.8.0                 # ONNX embeddings (FREE tier — no torch)
chromadb==0.5.0                  # local vector store
aiofiles==23.2.1                 # async file I/O for uploads

# Phase 3 — Retrieval + Generation
rank-bm25==0.2.2                 # BM25 retrieval
langchain>=0.3.0,<1.0.0          # RAG chain orchestration
langchain-google-genai>=2.0.0,<3.0.0   # Gemini LLM
langchain-openai>=0.2.0,<1.0.0   # OpenAI LLM (optional fallback)
langchain-groq>=0.3.0,<1.0.0     # Groq LLM (optional provider)

# Phase 7 — RAGAS Evaluation
ragas>=0.2.0,<0.3.0              # RAGAS evaluation framework
datasets>=2.14.0,<3              # HuggingFace datasets (ragas dependency)

# Testing
pytest==8.2.0
pytest-asyncio==0.23.7

# Dev utilities
python-dotenv==1.0.1
