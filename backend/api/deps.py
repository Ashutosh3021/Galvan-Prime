"""
api/deps.py — Shared FastAPI dependencies.

No database — the app is stateless.  Add any future shared dependencies here.
"""

# Nothing to export yet; kept so existing imports don't break if any route
# still references this module.
__all__: list[str] = []
