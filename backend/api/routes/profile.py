"""
api/routes/profile.py — Profile endpoints (Phase 5 stub).
"""

from fastapi import APIRouter

router = APIRouter(prefix="/profile", tags=["profile"])

# Phase 5 will implement:
#   GET /profile/me
#   PUT /profile/me
