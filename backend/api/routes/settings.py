"""
api/routes/settings.py — Settings endpoints (Phase 5 stub).
"""

from fastapi import APIRouter

router = APIRouter(prefix="/settings", tags=["settings"])

# Phase 5 will implement:
#   GET /settings
#   PUT /settings
